diff --git a/node_modules/@opentripplanner/otp2-tile-overlay/esm/index.js b/node_modules/@opentripplanner/otp2-tile-overlay/esm/index.js
index 5b8b96d..4aab7b7 100644
--- a/node_modules/@opentripplanner/otp2-tile-overlay/esm/index.js
+++ b/node_modules/@opentripplanner/otp2-tile-overlay/esm/index.js
@@ -63,9 +63,15 @@ var OTP2TileLayerWithPopup = function OTP2TileLayerWithPopup(_ref) {
     } : sourceLayer === "rentalStations" && "formFactors" in synthesizedEntity ? {
       formFactor: synthesizedEntity.formFactors
     } : undefined;
-    synthesizedEntity.rentalNetwork = "network" in synthesizedEntity ? {
-      networkId: synthesizedEntity.network
-    } : undefined;
+    // Assigning `undefined` still CREATES the key, and map-popup gates on
+    // `"rentalNetwork" in entity`. A tapped bus stop therefore looked like a
+    // rental vehicle and MapPopup threw on `rentalNetwork.networkId`,
+    // blanking the app mid-trip (2026-09-04). Only set it when there is one.
+    if ("network" in synthesizedEntity && synthesizedEntity.network) {
+      synthesizedEntity.rentalNetwork = { networkId: synthesizedEntity.network };
+    } else {
+      delete synthesizedEntity.rentalNetwork;
+    }
     if (sourceLayer === "rentalStations") {
       synthesizedEntity.availableVehicles = undefined;
       synthesizedEntity.availableSpaces = undefined;
diff --git a/node_modules/@opentripplanner/otp2-tile-overlay/lib/index.js b/node_modules/@opentripplanner/otp2-tile-overlay/lib/index.js
index 8662c3a..6f78746 100644
--- a/node_modules/@opentripplanner/otp2-tile-overlay/lib/index.js
+++ b/node_modules/@opentripplanner/otp2-tile-overlay/lib/index.js
@@ -65,9 +65,16 @@ const OTP2TileLayerWithPopup = ({
     } : sourceLayer === "rentalStations" && "formFactors" in synthesizedEntity ? {
       formFactor: synthesizedEntity.formFactors
     } : undefined;
-    synthesizedEntity.rentalNetwork = "network" in synthesizedEntity ? {
-      networkId: synthesizedEntity.network
-    } : undefined;
+    // Assigning `undefined` still CREATES the key, and map-popup gates on
+    // `"rentalNetwork" in entity`. A tapped bus stop therefore looked like a
+    // rental vehicle and MapPopup threw on `rentalNetwork.networkId`,
+    // blanking the app mid-trip (2026-09-04). Only set it when there is one.
+    if ("network" in synthesizedEntity && synthesizedEntity.network) {
+      synthesizedEntity.rentalNetwork = { networkId: synthesizedEntity.network };
+    }
+    else {
+      delete synthesizedEntity.rentalNetwork;
+    }
     if (sourceLayer === "rentalStations") {
       synthesizedEntity.availableVehicles = undefined;
       synthesizedEntity.availableSpaces = undefined;
diff --git a/node_modules/@opentripplanner/otp2-tile-overlay/src/index.tsx b/node_modules/@opentripplanner/otp2-tile-overlay/src/index.tsx
index d77aa79..e6f1f3e 100644
--- a/node_modules/@opentripplanner/otp2-tile-overlay/src/index.tsx
+++ b/node_modules/@opentripplanner/otp2-tile-overlay/src/index.tsx
@@ -127,10 +127,15 @@ const OTP2TileLayerWithPopup = ({
         : sourceLayer === "rentalStations" && "formFactors" in synthesizedEntity
         ? { formFactor: synthesizedEntity.formFactors }
         : undefined;
-    synthesizedEntity.rentalNetwork =
-      "network" in synthesizedEntity
-        ? { networkId: synthesizedEntity.network }
-        : undefined;
+    // Assigning `undefined` still CREATES the key, and map-popup gates on
+    // `"rentalNetwork" in entity`. A tapped bus stop therefore looked like a
+    // rental vehicle and MapPopup threw on `rentalNetwork.networkId`,
+    // blanking the app mid-trip (2026-09-04). Only set it when there is one.
+    if ("network" in synthesizedEntity && synthesizedEntity.network) {
+      synthesizedEntity.rentalNetwork = { networkId: synthesizedEntity.network };
+    } else {
+      delete synthesizedEntity.rentalNetwork;
+    }
     if (sourceLayer === "rentalStations") {
       synthesizedEntity.availableVehicles = undefined;
       synthesizedEntity.availableSpaces = undefined;

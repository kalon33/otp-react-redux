diff --git a/node_modules/@opentripplanner/trip-details/esm/components/fares-v2-table.js b/node_modules/@opentripplanner/trip-details/esm/components/fares-v2-table.js
index ee40370..cf43160 100644
--- a/node_modules/@opentripplanner/trip-details/esm/components/fares-v2-table.js
+++ b/node_modules/@opentripplanner/trip-details/esm/components/fares-v2-table.js
@@ -126,7 +126,7 @@ var FaresV2Table = function FaresV2Table(_ref) {
     scope: "col"
   }, /*#__PURE__*/React.createElement("em", null, /*#__PURE__*/React.createElement(FormattedMessage, {
     id: "otpUi.TripDetails.total"
-  }))) : /*#__PURE__*/React.createElement(React.Fragment, null)])];
+  }))) : /*#__PURE__*/React.createElement(React.Fragment, null, null)])];
 
   // non-transit legs don't have fares
   transitLegs.forEach(function (leg, index) {
@@ -162,7 +162,7 @@ var FaresV2Table = function FaresV2Table(_ref) {
         style: {
           textAlign: legPrice ? "right" : "center"
         },
-        title: !Number.isNaN(originalAmount) && originalAmount > 0 && index > 0 && intl.formatMessage({
+        title: !Number.isNaN(originalAmount) && originalAmount > 0 && index > 0 ? intl.formatMessage({
           description: "Text explaining the transfer discount applied to this fare.",
           id: "otpUi.TripDetails.transferDiscountExplanation"
         }, {
@@ -172,9 +172,9 @@ var FaresV2Table = function FaresV2Table(_ref) {
             style: "currency"
           })
         })
-      }, !Number.isNaN(originalAmount) && originalAmount > 0 && index > 0 && /*#__PURE__*/React.createElement(TransferIcon, {
+      }, /*#__PURE__*/React.createElement(React.Fragment, null, !Number.isNaN(originalAmount) && originalAmount > 0 && index > 0 && /*#__PURE__*/React.createElement(TransferIcon, {
         size: 16
-      }), legPrice ? renderFare(legPrice === null || legPrice === void 0 || (_legPrice$currency2 = legPrice.currency) === null || _legPrice$currency2 === void 0 ? void 0 : _legPrice$currency2.code, legPrice === null || legPrice === void 0 ? void 0 : legPrice.amount) : FailDash));
+      })), legPrice ? renderFare(legPrice === null || legPrice === void 0 || (_legPrice$currency2 = legPrice.currency) === null || _legPrice$currency2 === void 0 ? void 0 : _legPrice$currency2.code, legPrice === null || legPrice === void 0 ? void 0 : legPrice.amount) : FailDash));
       var nextRowIndex = currentRowIndex + 1;
       if (!rows[nextRowIndex]) {
         rows[nextRowIndex] = [];
@@ -198,11 +198,15 @@ var FaresV2Table = function FaresV2Table(_ref) {
   });
   return /*#__PURE__*/React.createElement(StripedTable, null, /*#__PURE__*/React.createElement(InvisibleA11yCaption, null, /*#__PURE__*/React.createElement(FormattedMessage, {
     id: "otpUi.TripDetails.transitFareTable"
-  })), rows.map(function (r, index) {
+  })), /*#__PURE__*/React.createElement("tbody", null, rows.map(function (r, rowIndex) {
     return /*#__PURE__*/React.createElement("tr", {
-      key: index
-    }, r);
-  }));
+      key: "row-" + rowIndex
+    }, r.map(function (cell, cellIndex) {
+      return /*#__PURE__*/React.cloneElement(cell, {
+        key: "cell-" + rowIndex + "-" + cellIndex
+      });
+    }));
+  })));
 };
 export default FaresV2Table;
 //# sourceMappingURL=fares-v2-table.js.map
\ No newline at end of file
diff --git a/node_modules/@opentripplanner/trip-details/src/components/fares-v2-table.tsx b/node_modules/@opentripplanner/trip-details/src/components/fares-v2-table.tsx
index b522f41..d2eae02 100644
--- a/node_modules/@opentripplanner/trip-details/src/components/fares-v2-table.tsx
+++ b/node_modules/@opentripplanner/trip-details/src/components/fares-v2-table.tsx
@@ -1,5 +1,5 @@
 /* eslint-disable no-underscore-dangle */
-import React from "react";
+import React, { Fragment } from "react";
 import { Leg } from "@opentripplanner/types";
 import coreUtils from "@opentripplanner/core-utils";
 import { FormattedMessage, useIntl } from "react-intl";
@@ -146,7 +146,7 @@ const FaresV2Table = ({
           </em>
         </th>
       ) : (
-        <></>
+        <Fragment />
       )
     ]
   ];
@@ -172,7 +172,7 @@ const FaresV2Table = ({
 
       // Only consider alternateFareProducts if current product is dependent
       const dependentAlternateFareProducts =
-        isDependent && alternateFareProducts;
+        isDependent ? alternateFareProducts : null;
 
       // Calculate pre-tranfer amount either via alternate fare or fare-id matching (price.originalAmount)
       const originalAmount =
@@ -185,15 +185,16 @@ const FaresV2Table = ({
           {index === 0 && (
             <th style={{ textAlign: "left" }} scope="row">
               {medium?.id && (
-                <>
+                <span key="medium-name">
                   <FormattedMessage
                     id={`config.fares.media.${descope(medium.id)}`}
                     defaultMessage={medium.name}
                   />{" "}
-                </>
+                </span>
               )}
               {rider?.id && (
                 <FormattedMessage
+                  key="rider-name"
                   id={`config.fares.riderCategory.${descope(rider.id)}`}
                   defaultMessage={rider.name}
                 />
@@ -206,17 +207,17 @@ const FaresV2Table = ({
             title={
               !Number.isNaN(originalAmount) &&
               originalAmount > 0 &&
-              index > 0 &&
-              intl.formatMessage(
-                {
-                  description:
-                    "Text explaining the transfer discount applied to this fare.",
-                  id: "otpUi.TripDetails.transferDiscountExplanation"
-                },
-                {
-                  transferAmount: intl.formatNumber(originalAmount, {
-                    currency: legPrice?.currency?.code,
-                    currencyDisplay: "narrowSymbol",
+              index > 0
+                ? intl.formatMessage(
+                    {
+                      description:
+                        "Text explaining the transfer discount applied to this fare.",
+                      id: "otpUi.TripDetails.transferDiscountExplanation"
+                    },
+                    {
+                      transferAmount: intl.formatNumber(originalAmount, {
+                        currency: legPrice?.currency?.code,
+                        currencyDisplay: "narrowSymbol",
                     style: "currency"
                   })
                 }
@@ -273,9 +274,17 @@ const FaresV2Table = ({
       <InvisibleA11yCaption>
         <FormattedMessage id="otpUi.TripDetails.transitFareTable" />
       </InvisibleA11yCaption>
-      {rows.map((r, index) => (
-        <tr key={index}>{r}</tr>
-      ))}
+      <tbody>
+        {rows.map((r, rowIndex) => (
+          <tr key={`row-${rowIndex}`}>
+            {r.map((cell, cellIndex) => (
+              <Fragment key={`cell-${rowIndex}-${cellIndex}`}>
+                {cell}
+              </Fragment>
+            ))}
+          </tr>
+        ))}
+      </tbody>
     </StripedTable>
   );
 };

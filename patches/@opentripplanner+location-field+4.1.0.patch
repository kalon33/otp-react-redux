diff --git a/node_modules/@opentripplanner/location-field/src/index.tsx b/node_modules/@opentripplanner/location-field/src/index.tsx
index 61d3576..dc8c2db 100644
--- a/node_modules/@opentripplanner/location-field/src/index.tsx
+++ b/node_modules/@opentripplanner/location-field/src/index.tsx
@@ -730,6 +730,28 @@ const LocationField = ({
     indexedOptionLookup.push({ id, locationSelected });
   };
 
+  // TransitNav fork (backlog 3.9 / 3.10): `pinCurrentLocationFirst` moves the
+  // current-location entry from the bottom of the list to the top, and
+  // `mapPickOption` adds one extra entry ("Choose on map") directly beneath it.
+  // Their keyboard handlers are registered here, ahead of every other category,
+  // so the arrow-key order matches the order the rider sees; the option elements
+  // themselves are still built at the end of the list and moved to the front
+  // once every other category has been added.
+  const pinnedMenuItems = [];
+  if (pinCurrentLocationFirst) {
+    if (!suppressNearby) {
+      pushToIndexedOptions(
+        currentPosition && !currentPosition.error
+          ? useCurrentLocation
+          : undefined,
+        "current-position"
+      );
+    }
+    if (mapPickOption) {
+      pushToIndexedOptions(mapPickOption.onClick, "choose-on-map");
+    }
+  }
+
   /* 0) Include user saved locations if the typed text contains those locations name. */
   if (showUserSettings) {
     const matchingLocations = getMatchingLocations(
@@ -1044,6 +1066,38 @@ const LocationField = ({
       />
     );
   }
+
+  if (mapPickOption) {
+    if (!pinCurrentLocationFirst) {
+      pushToIndexedOptions(mapPickOption.onClick, "choose-on-map");
+    }
+    (pinCurrentLocationFirst ? pinnedMenuItems : menuItems).push(
+      <Option
+        icon={mapPickOption.icon}
+        id="choose-on-map"
+        isActive={indexedOptionLookup[activeIndex]?.id === "choose-on-map"}
+        key="choose-on-map"
+        onClick={mapPickOption.onClick}
+        title={mapPickOption.title}
+      />
+    );
+  }
+
+  if (mapPickOption) {
+    if (!pinCurrentLocationFirst) {
+      pushToIndexedOptions(mapPickOption.onClick, "choose-on-map");
+    }
+    (pinCurrentLocationFirst ? pinnedMenuItems : menuItems).push(
+      <Option
+        icon={mapPickOption.icon}
+        id="choose-on-map"
+        isActive={indexedOptionLookup[activeIndex]?.id === "choose-on-map"}
+        key="choose-on-map"
+        onClick={mapPickOption.onClick}
+        title={mapPickOption.title}
+      />
+    );
+  }
   if (message && !message.includes("AbortError")) {
     if (geocodedFeatures.length === 0) {
       const icon = isFetching ? (
@@ -1058,6 +1112,18 @@ const LocationField = ({
     statusMessages.push(message);
   }
 
+  // Put the pinned options in front of everything else, including the geocoder
+  // error row that is unshifted just above.
+  if (pinnedMenuItems.length > 0) {
+    menuItems = [...pinnedMenuItems, ...menuItems];
+  }
+
+  // Put the pinned options in front of everything else, including the geocoder
+  // error row that is unshifted just above.
+  if (pinnedMenuItems.length > 0) {
+    menuItems = [...pinnedMenuItems, ...menuItems];
+  }
+
   // Store the number of location-associated items for reference in the onKeyDown method
   let menuItemCount = indexedOptionLookup.length;
 
diff --git a/node_modules/@opentripplanner/location-field/src/types.ts b/node_modules/@opentripplanner/location-field/src/types.ts
index 51c88b3..7f1e708 100644
--- a/node_modules/@opentripplanner/location-field/src/types.ts
+++ b/node_modules/@opentripplanner/location-field/src/types.ts
@@ -279,6 +279,20 @@ export interface LocationFieldProps {
    * If true, do not show nearbyStops or current location as options
    */
   suppressNearby?: boolean;
+  /**
+   * TransitNav fork: an extra option rendered directly under the current-location
+   * option ("Choose on map"). Omit to render nothing.
+   */
+  mapPickOption?: {
+    icon?: React.ReactNode;
+    onClick: () => void;
+    title: string;
+  };
+  /**
+   * TransitNav fork: render the current-location option (and mapPickOption) at the
+   * top of the suggestion list instead of the bottom.
+   */
+  pinCurrentLocationFirst?: boolean;
   /**
    * A custom component for rendering the icon for options that are either saved
    * user locations or recent places. The component will be sent a single prop

diff --git a/node_modules/@opentripplanner/location-field/esm/index.js b/node_modules/@opentripplanner/location-field/esm/index.js
index 153c58e..8eb02d8 100644
--- a/node_modules/@opentripplanner/location-field/esm/index.js
+++ b/node_modules/@opentripplanner/location-field/esm/index.js
@@ -229,6 +229,8 @@ var LocationField = function LocationField(_ref4) {
     _ref4$LocationIconCom = _ref4.LocationIconComponent,
     LocationIconComponent = _ref4$LocationIconCom === void 0 ? DefaultLocationIcon : _ref4$LocationIconCom,
     locationType = _ref4.locationType,
+    _ref4$mapPickOption = _ref4.mapPickOption,
+    mapPickOption = _ref4$mapPickOption === void 0 ? null : _ref4$mapPickOption,
     _ref4$nearbyStops = _ref4.nearbyStops,
     nearbyStops = _ref4$nearbyStops === void 0 ? [] : _ref4$nearbyStops,
     _ref4$onBlur = _ref4.onBlur,
@@ -240,6 +242,10 @@ var LocationField = function LocationField(_ref4) {
     onTextInputClick = _ref4$onTextInputClic === void 0 ? null : _ref4$onTextInputClic,
     _ref4$operatorIconMap = _ref4.operatorIconMap,
     operatorIconMap = _ref4$operatorIconMap === void 0 ? {} : _ref4$operatorIconMap,
+    _ref4$outputMetricUnits = _ref4.outputMetricUnits,
+    outputMetricUnits = _ref4$outputMetricUnits === void 0 ? false : _ref4$outputMetricUnits,
+    _ref4$pinCurrentLocFirst = _ref4.pinCurrentLocationFirst,
+    pinCurrentLocationFirst = _ref4$pinCurrentLocFirst === void 0 ? false : _ref4$pinCurrentLocFirst,
     _ref4$preferredLayers = _ref4.preferredLayers,
     preferredLayers = _ref4$preferredLayers === void 0 ? [] : _ref4$preferredLayers,
     _ref4$sessionOptionIc = _ref4.sessionOptionIcon,
@@ -640,6 +646,22 @@ var LocationField = function LocationField(_ref4) {
       locationSelected: locationSelected
     });
   };
+  // TransitNav fork (backlog 3.9 / 3.10): `pinCurrentLocationFirst` moves the
+  // current-location entry from the bottom of the list to the top, and
+  // `mapPickOption` adds one extra entry ("Choose on map") directly beneath it.
+  // Their keyboard handlers are registered here, ahead of every other category,
+  // so the arrow-key order matches the order the rider sees; the option elements
+  // themselves are still built at the end of the list and moved to the front
+  // once every other category has been added.
+  var pinnedMenuItems = [];
+  if (pinCurrentLocationFirst) {
+    if (!suppressNearby) {
+      pushToIndexedOptions(currentPosition && !currentPosition.error ? useCurrentLocation : undefined, "current-position");
+    }
+    if (mapPickOption) {
+      pushToIndexedOptions(mapPickOption.onClick, "choose-on-map");
+    }
+  }
 
   /* 0) Include user saved locations if the typed text contains those locations name. */
   if (showUserSettings) {
@@ -785,9 +807,11 @@ var LocationField = function LocationField(_ref4) {
       // Create and return the option menu item
       return /*#__PURE__*/React.createElement(TransitStopOption, {
         id: stopId,
+        intl: intl,
         isActive: ((_indexedOptionLookup$4 = indexedOptionLookup[activeIndex]) === null || _indexedOptionLookup$4 === void 0 ? void 0 : _indexedOptionLookup$4.id) === stopId,
         key: stopId,
         onClick: locationSelected,
+        outputMetricUnits: outputMetricUnits,
         stop: stop,
         stopOptionIcon: stopOptionIcon
       });
@@ -879,12 +903,17 @@ var LocationField = function LocationField(_ref4) {
   }
   var optionId = "current-position";
 
-  // Add to the selection handler lookup (for use in onKeyDown)
-  pushToIndexedOptions(locationSelected, optionId);
+  // Add to the selection handler lookup (for use in onKeyDown). Skipped when
+  // the option is pinned (already registered above) and when it is suppressed
+  // (registering a handler for an option that is never rendered shifts every
+  // arrow-key index by one).
+  if (!pinCurrentLocationFirst && !suppressNearby) {
+    pushToIndexedOptions(locationSelected, optionId);
+  }
   if (!suppressNearby) {
     var _indexedOptionLookup$6;
     // Create and add the option item to the menu items array
-    menuItems.push(/*#__PURE__*/React.createElement(Option, {
+    (pinCurrentLocationFirst ? pinnedMenuItems : menuItems).push(/*#__PURE__*/React.createElement(Option, {
       disabled: positionUnavailable,
       icon: optionIcon,
       id: optionId,
@@ -894,6 +923,20 @@ var LocationField = function LocationField(_ref4) {
       title: optionTitle
     }));
   }
+  if (mapPickOption) {
+    var _indexedOptionLookup$mp;
+    if (!pinCurrentLocationFirst) {
+      pushToIndexedOptions(mapPickOption.onClick, "choose-on-map");
+    }
+    (pinCurrentLocationFirst ? pinnedMenuItems : menuItems).push(/*#__PURE__*/React.createElement(Option, {
+      icon: mapPickOption.icon,
+      id: "choose-on-map",
+      isActive: ((_indexedOptionLookup$mp = indexedOptionLookup[activeIndex]) === null || _indexedOptionLookup$mp === void 0 ? void 0 : _indexedOptionLookup$mp.id) === "choose-on-map",
+      key: "choose-on-map",
+      onClick: mapPickOption.onClick,
+      title: mapPickOption.title
+    }));
+  }
   if (message && !message.includes("AbortError")) {
     if (geocodedFeatures.length === 0) {
       var icon = isFetching ? /*#__PURE__*/React.createElement(S.Spinner, {
@@ -911,6 +954,12 @@ var LocationField = function LocationField(_ref4) {
     statusMessages.push(message);
   }
 
+  // Put the pinned options in front of everything else, including the geocoder
+  // error row that is unshifted just above.
+  if (pinnedMenuItems.length > 0) {
+    menuItems = [].concat(pinnedMenuItems, menuItems);
+  }
+
   // Store the number of location-associated items for reference in the onKeyDown method
   var menuItemCount = indexedOptionLookup.length;
 
diff --git a/node_modules/@opentripplanner/location-field/esm/options.js b/node_modules/@opentripplanner/location-field/esm/options.js
index bb1b2ce..a759b6d 100644
--- a/node_modules/@opentripplanner/location-field/esm/options.js
+++ b/node_modules/@opentripplanner/location-field/esm/options.js
@@ -1,4 +1,3 @@
-import { humanizeDistanceStringImperial } from "@opentripplanner/humanize-distance";
 import React from "react";
 import { Bus } from "@styled-icons/fa-solid/Bus";
 import { Briefcase } from "@styled-icons/fa-solid/Briefcase";
@@ -85,10 +84,42 @@ export function Option(_ref3) {
     }, /*#__PURE__*/React.createElement(S.HiddenContent, null, ", "), label);
   })))));
 }
+function formatStopDistance(meters, outputMetricUnits, intl) {
+  if (meters == null || isNaN(meters)) return "";
+  var unit, value;
+  if (outputMetricUnits) {
+    var km = meters / 1000;
+    if (km >= 1) {
+      unit = "kilometer";
+      value = km >= 100 ? Math.round(km) : Math.round(km * 10) / 10;
+    } else {
+      unit = "meter";
+      value = Math.round(meters);
+    }
+  } else {
+    var yards = meters * 1.09361;
+    if (yards >= 1760) {
+      unit = "mile";
+      value = Math.round((yards / 1760) * 10) / 10;
+    } else {
+      unit = "yard";
+      value = Math.round(yards);
+    }
+  }
+  if (intl && typeof intl.formatNumber === "function") {
+    return intl.formatNumber(value, { style: "unit", unit: unit, unitDisplay: "short" });
+  }
+  var shortUnit = unit === "meter" ? "m" : unit === "kilometer" ? "km" : unit === "yard" ? "yd" : "mi";
+  return "".concat(value, " ").concat(shortUnit);
+}
 export function TransitStopOption(_ref4) {
   var id = _ref4.id,
     _ref4$isActive = _ref4.isActive,
     isActive = _ref4$isActive === void 0 ? false : _ref4$isActive,
+    _ref4$intl = _ref4.intl,
+    intl = _ref4$intl === void 0 ? null : _ref4$intl,
+    _ref4$outputMetricUnits = _ref4.outputMetricUnits,
+    outputMetricUnits = _ref4$outputMetricUnits === void 0 ? false : _ref4$outputMetricUnits,
     onClick = _ref4.onClick,
     stop = _ref4.stop,
     stopOptionIcon = _ref4.stopOptionIcon;
@@ -96,7 +127,7 @@ export function TransitStopOption(_ref4) {
     active: isActive,
     id: id,
     onClick: onClick
-  }, /*#__PURE__*/React.createElement(S.StopIconAndDistanceContainer, null, stopOptionIcon, /*#__PURE__*/React.createElement(S.StopDistance, null, humanizeDistanceStringImperial(stop.dist, true))), /*#__PURE__*/React.createElement(S.StopContentContainer, null, /*#__PURE__*/React.createElement(S.StopName, null, stop.name, " (", stop.code, ")"), /*#__PURE__*/React.createElement(S.StopRoutes, null, (stop.routes || []).map(function (route) {
+  }, /*#__PURE__*/React.createElement(S.StopIconAndDistanceContainer, null, stopOptionIcon, /*#__PURE__*/React.createElement(S.StopDistance, null, formatStopDistance(stop.dist, outputMetricUnits, intl))), /*#__PURE__*/React.createElement(S.StopContentContainer, null, /*#__PURE__*/React.createElement(S.StopName, null, stop.name, " (", stop.code, ")"), /*#__PURE__*/React.createElement(S.StopRoutes, null, (stop.routes || []).map(function (route) {
     var name = route.shortName || route.longName;
     return /*#__PURE__*/React.createElement(S.RouteName, {
       key: "route-".concat(name)
diff --git a/node_modules/@opentripplanner/location-field/lib/index.js b/node_modules/@opentripplanner/location-field/lib/index.js
index 1b70e99..e0c6c9a 100644
--- a/node_modules/@opentripplanner/location-field/lib/index.js
+++ b/node_modules/@opentripplanner/location-field/lib/index.js
@@ -216,12 +216,15 @@ const LocationField = ({
   location = null,
   LocationIconComponent = DefaultLocationIcon,
   locationType,
+  mapPickOption = null,
   nearbyStops = [],
   onBlur = null,
   onFocus = null,
   onLocationSelected,
   onTextInputClick = null,
   operatorIconMap = {},
+  outputMetricUnits = false,
+  pinCurrentLocationFirst = false,
   preferredLayers = [],
   sessionOptionIcon = /*#__PURE__*/_react.default.createElement(_Search.Search, {
     size: _options.ICON_SIZE
@@ -590,6 +593,22 @@ const LocationField = ({
       locationSelected
     });
   };
+  // TransitNav fork (backlog 3.9 / 3.10): `pinCurrentLocationFirst` moves the
+  // current-location entry from the bottom of the list to the top, and
+  // `mapPickOption` adds one extra entry ("Choose on map") directly beneath it.
+  // Their keyboard handlers are registered here, ahead of every other category,
+  // so the arrow-key order matches the order the rider sees; the option elements
+  // themselves are still built at the end of the list and moved to the front
+  // once every other category has been added.
+  const pinnedMenuItems = [];
+  if (pinCurrentLocationFirst) {
+    if (!suppressNearby) {
+      pushToIndexedOptions(currentPosition && !currentPosition.error ? useCurrentLocation : undefined, "current-position");
+    }
+    if (mapPickOption) {
+      pushToIndexedOptions(mapPickOption.onClick, "choose-on-map");
+    }
+  }
 
   /* 0) Include user saved locations if the typed text contains those locations name. */
   if (showUserSettings) {
@@ -733,9 +752,11 @@ const LocationField = ({
       // Create and return the option menu item
       return /*#__PURE__*/_react.default.createElement(_options.TransitStopOption, {
         id: stopId,
+        intl: intl,
         isActive: ((_indexedOptionLookup$4 = indexedOptionLookup[activeIndex]) === null || _indexedOptionLookup$4 === void 0 ? void 0 : _indexedOptionLookup$4.id) === stopId,
         key: stopId,
         onClick: locationSelected,
+        outputMetricUnits: outputMetricUnits,
         stop: stop,
         stopOptionIcon: stopOptionIcon
       });
@@ -825,12 +846,17 @@ const LocationField = ({
   }
   const optionId = `current-position`;
 
-  // Add to the selection handler lookup (for use in onKeyDown)
-  pushToIndexedOptions(locationSelected, optionId);
+  // Add to the selection handler lookup (for use in onKeyDown). Skipped when
+  // the option is pinned (already registered above) and when it is suppressed
+  // (registering a handler for an option that is never rendered shifts every
+  // arrow-key index by one).
+  if (!pinCurrentLocationFirst && !suppressNearby) {
+    pushToIndexedOptions(locationSelected, optionId);
+  }
   if (!suppressNearby) {
     var _indexedOptionLookup$6;
     // Create and add the option item to the menu items array
-    menuItems.push(/*#__PURE__*/_react.default.createElement(_options.Option, {
+    (pinCurrentLocationFirst ? pinnedMenuItems : menuItems).push(/*#__PURE__*/_react.default.createElement(_options.Option, {
       disabled: positionUnavailable,
       icon: optionIcon,
       id: optionId,
@@ -840,6 +866,20 @@ const LocationField = ({
       title: optionTitle
     }));
   }
+  if (mapPickOption) {
+    var _indexedOptionLookup$mp;
+    if (!pinCurrentLocationFirst) {
+      pushToIndexedOptions(mapPickOption.onClick, "choose-on-map");
+    }
+    (pinCurrentLocationFirst ? pinnedMenuItems : menuItems).push(/*#__PURE__*/_react.default.createElement(_options.Option, {
+      icon: mapPickOption.icon,
+      id: "choose-on-map",
+      isActive: ((_indexedOptionLookup$mp = indexedOptionLookup[activeIndex]) === null || _indexedOptionLookup$mp === void 0 ? void 0 : _indexedOptionLookup$mp.id) === "choose-on-map",
+      key: "choose-on-map",
+      onClick: mapPickOption.onClick,
+      title: mapPickOption.title
+    }));
+  }
   if (message && !message.includes("AbortError")) {
     if (geocodedFeatures.length === 0) {
       const icon = isFetching ? /*#__PURE__*/_react.default.createElement(S.Spinner, {
@@ -857,6 +897,12 @@ const LocationField = ({
     statusMessages.push(message);
   }
 
+  // Put the pinned options in front of everything else, including the geocoder
+  // error row that is unshifted just above.
+  if (pinnedMenuItems.length > 0) {
+    menuItems = [...pinnedMenuItems, ...menuItems];
+  }
+
   // Store the number of location-associated items for reference in the onKeyDown method
   let menuItemCount = indexedOptionLookup.length;
 
diff --git a/node_modules/@opentripplanner/location-field/lib/options.js b/node_modules/@opentripplanner/location-field/lib/options.js
index 80348d8..70ec03e 100644
--- a/node_modules/@opentripplanner/location-field/lib/options.js
+++ b/node_modules/@opentripplanner/location-field/lib/options.js
@@ -11,7 +11,6 @@ exports.TransitStopOption = TransitStopOption;
 exports.UserLocationIcon = UserLocationIcon;
 exports.getRenderData = getRenderData;
 exports.getStoredPlaceName = getStoredPlaceName;
-var _humanizeDistance = require("@opentripplanner/humanize-distance");
 var _react = _interopRequireDefault(require("react"));
 var _Bus = require("@styled-icons/fa-solid/Bus");
 var _Briefcase = require("@styled-icons/fa-solid/Briefcase");
@@ -90,9 +89,39 @@ function Option({
     key: idx
   }, /*#__PURE__*/_react.default.createElement(S.HiddenContent, null, ", "), label))))));
 }
+function formatStopDistance(meters, outputMetricUnits, intl) {
+  if (meters == null || isNaN(meters)) return "";
+  let unit, value;
+  if (outputMetricUnits) {
+    const km = meters / 1000;
+    if (km >= 1) {
+      unit = "kilometer";
+      value = km >= 100 ? Math.round(km) : Math.round(km * 10) / 10;
+    } else {
+      unit = "meter";
+      value = Math.round(meters);
+    }
+  } else {
+    const yards = meters * 1.09361;
+    if (yards >= 1760) {
+      unit = "mile";
+      value = Math.round((yards / 1760) * 10) / 10;
+    } else {
+      unit = "yard";
+      value = Math.round(yards);
+    }
+  }
+  if (intl && typeof intl.formatNumber === "function") {
+    return intl.formatNumber(value, { style: "unit", unit, unitDisplay: "short" });
+  }
+  const shortUnit = unit === "meter" ? "m" : unit === "kilometer" ? "km" : unit === "yard" ? "yd" : "mi";
+  return `${value} ${shortUnit}`;
+}
 function TransitStopOption({
   id,
   isActive = false,
+  intl = null,
+  outputMetricUnits = false,
   onClick,
   stop,
   stopOptionIcon
@@ -101,7 +130,7 @@ function TransitStopOption({
     active: isActive,
     id: id,
     onClick: onClick
-  }, /*#__PURE__*/_react.default.createElement(S.StopIconAndDistanceContainer, null, stopOptionIcon, /*#__PURE__*/_react.default.createElement(S.StopDistance, null, (0, _humanizeDistance.humanizeDistanceStringImperial)(stop.dist, true))), /*#__PURE__*/_react.default.createElement(S.StopContentContainer, null, /*#__PURE__*/_react.default.createElement(S.StopName, null, stop.name, " (", stop.code, ")"), /*#__PURE__*/_react.default.createElement(S.StopRoutes, null, (stop.routes || []).map(route => {
+  }, /*#__PURE__*/_react.default.createElement(S.StopIconAndDistanceContainer, null, stopOptionIcon, /*#__PURE__*/_react.default.createElement(S.StopDistance, null, formatStopDistance(stop.dist, outputMetricUnits, intl))), /*#__PURE__*/_react.default.createElement(S.StopContentContainer, null, /*#__PURE__*/_react.default.createElement(S.StopName, null, stop.name, " (", stop.code, ")"), /*#__PURE__*/_react.default.createElement(S.StopRoutes, null, (stop.routes || []).map(route => {
     const name = route.shortName || route.longName;
     return /*#__PURE__*/_react.default.createElement(S.RouteName, {
       key: `route-${name}`
diff --git a/node_modules/@opentripplanner/location-field/lib/types.d.ts b/node_modules/@opentripplanner/location-field/lib/types.d.ts
index f520750..ee7ecdc 100644
--- a/node_modules/@opentripplanner/location-field/lib/types.d.ts
+++ b/node_modules/@opentripplanner/location-field/lib/types.d.ts
@@ -273,6 +273,25 @@ export interface LocationFieldProps {
      * If true, do not show nearbyStops or current location as options
      */
     suppressNearby?: boolean;
+    /**
+     * TransitNav fork: an extra option rendered directly under the current-location
+     * option ("Choose on map"). Omit to render nothing.
+     */
+    mapPickOption?: {
+      icon?: React.ReactNode;
+      onClick: () => void;
+      title: string;
+    };
+    /**
+     * TransitNav fork: render the current-location option (and mapPickOption) at the
+     * top of the suggestion list instead of the bottom.
+     */
+    pinCurrentLocationFirst?: boolean;
+    /**
+     * TransitNav fork: render nearby-stop distances in metric units (true) or
+     * imperial units (false). Defaults to false (imperial).
+     */
+    outputMetricUnits?: boolean;
     /**
      * A custom component for rendering the icon for options that are either saved
      * user locations or recent places. The component will be sent a single prop

diff --git a/node_modules/@opentripplanner/transitive-overlay/esm/index.js b/node_modules/@opentripplanner/transitive-overlay/esm/index.js
index 8522217..5d760a6 100644
--- a/node_modules/@opentripplanner/transitive-overlay/esm/index.js
+++ b/node_modules/@opentripplanner/transitive-overlay/esm/index.js
@@ -117,6 +117,11 @@ var loadImages = function loadImages(map, images) {
     if (!map.hasImage(img.id)) {
       // Only load if the image hasn't already been added
       map.loadImage(img.url).then(function (response) {
+        // The guard above ran synchronously and this runs a microtask
+        // later, so two effects mounting in the same frame both pass it
+        // and the second addImage throws "An image named ... already
+        // exists": 18 of them per map mount on 2026-08-31.
+        if (map.hasImage(img.id)) return;
         map.addImage(img.id, response.data, img.options);
       })["catch"](function (error) {
         // eslint-disable-next-line no-console
diff --git a/node_modules/@opentripplanner/transitive-overlay/lib/index.js b/node_modules/@opentripplanner/transitive-overlay/lib/index.js
index 3db0bab..80aeba0 100644
--- a/node_modules/@opentripplanner/transitive-overlay/lib/index.js
+++ b/node_modules/@opentripplanner/transitive-overlay/lib/index.js
@@ -128,6 +128,11 @@ const loadImages = (map, images) => {
     if (!map.hasImage(img.id)) {
       // Only load if the image hasn't already been added
       map.loadImage(img.url).then(response => {
+        // The guard above ran synchronously and this runs a microtask
+        // later, so two effects mounting in the same frame both pass it
+        // and the second addImage throws "An image named ... already
+        // exists": 18 of them per map mount on 2026-08-31.
+        if (map.hasImage(img.id)) return;
         map.addImage(img.id, response.data, img.options);
       }).catch(error => {
         // eslint-disable-next-line no-console
diff --git a/node_modules/@opentripplanner/transitive-overlay/src/index.tsx b/node_modules/@opentripplanner/transitive-overlay/src/index.tsx
index 8b23995..dcd323d 100644
--- a/node_modules/@opentripplanner/transitive-overlay/src/index.tsx
+++ b/node_modules/@opentripplanner/transitive-overlay/src/index.tsx
@@ -159,6 +159,11 @@ const loadImages = (map: MapRef, images: MapImage[]) => {
       map
         .loadImage(img.url)
         .then(response => {
+          // The guard above ran synchronously and this runs a microtask
+          // later, so two effects mounting in the same frame both pass it
+          // and the second addImage throws "An image named ... already
+          // exists": 18 of them per map mount on 2026-08-31.
+          if (map.hasImage(img.id)) return;
           map.addImage(img.id, response.data, img.options);
         })
         .catch(error => {
